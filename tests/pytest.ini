[pytest]
filterwarnings =
    error
    # Starlette 0.41 TestClient still uses an alias that anyio deprecated; not our code.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
